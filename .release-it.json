{
  "npm": {
    "publish": true,
    "skipChecks": true,
    "access": "public"
  },
  "git": {
    "requireBranch": "master",
    "tagName": "v${version}",
    "commitMessage": "Release v${version}",
    "tagAnnotation": "Release v${version}"
  },
  "github": {
    "release": true,
    "token": "${GITHUB_TOKEN}"
  }
}
